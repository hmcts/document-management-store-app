java:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    # Endpoints
    IDAM_USER_BASE_URI: https://idam-api.preview.platform.hmcts.net
    IDAM_S2S_BASE_URI: http://rpe-service-auth-provider-aat

    # logging vars & healthcheck
    REFORM_ENVIRONMENT: aat
    PACKAGES_ENVIRONMENT: aat

postgresql:
  enabled: true
dm-store:
  blobstorage:
    enabled: false
