spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/dm,/mnt/secrets/rpa
      aliases:
#        dm-store-POSTGRES-PASS: spring.datasource.password
        dm.dm-store-storage-account-primary-connection-string: azure.storage.connection-string
        dm.store-POSTGRES-PASS: spring.datasource.password
        rpa.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
